class Player {
  // some properties: name, score (numberOfMove) 
  // one possible method: makeMove
   constructor(player_name, player_number, player_type){
  	this.name = player_name;
  	this.number = player_number;
  	this.type = player_type;
  }

}